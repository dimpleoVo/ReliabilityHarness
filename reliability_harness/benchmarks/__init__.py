"""
reliability_harness.benchmarks — benchmark adapter layer.

Provides the canonical task schema and per-benchmark adapters for
ReliabilityHarness paper experiments (MBPP, HumanEval, and future benchmarks).

Entry point for formal experiments:
    python -m reliability_harness.experiments.run_benchmark --benchmark mbpp --dry-run
"""
